<% if post.collected_users.any? %>
  <%= post.collected_users.map{ |u| u.display_name }.join(",") %> 已收藏
<% end %>

<% if current_user # 有登入才可以收藏 %>
  <% if post.find_collect(current_user) %>
    <%= link_to "♡", uncollect_post_path(post), :method => :post,:remote => true,:class => "btn btn-ls btn-success" %>
  <% else %>
    <%= link_to "❤收藏", collect_post_path(post), :method => :post,:remote => true, :class => "btn btn-primary" %>
  <% end %>
<% end %>
